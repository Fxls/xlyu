-1- 简要描述HTML文档的结构
   <!DOCTYPE html> ---声明html类型，这里是html5
   <html> ---根元素
   <head> ---头部元素容器
   <head/>
   <body> ---主体
   <body/>
   <html/>
-2- 简要描述行内元素和块级元素的区别
    行内元素，只占用具体的元素的规格，不占用整行网页；
    块级元素，不论块中元素具体大小，都占用一整行网页
-3- 锚点的作用？如何创建锚点？
    锚点类似于超链接，在网页的某个位置留下一个标记，在设定的位置
    可以返回之前标记的位置
    创建方式：
    <a name = "xxx">111<a/>
    <a href = "#xxx">返回111<a/>
-4- table.html