-1-   A、错误，是new关键字创建对象，对象指向常量池中的字符串，如果常量池中没有，则在常量池中创建字符串
      B、正确，
      C、错误,
      D、错误，输出结果是true
-2-   A、错误，字符串中的单个字符长度都是两个字节
      B、错误，Unicode编码任何一个字符都是两个字节，长度为1
      C、正确
      D、错误。"你好String"用length()计算长度为8
-3-   TaskNo3.java（lastIndex）
-4-   trim的作用是去除字符串两端的空格，这样的意义是避免用户的输入失误带来影响输入失误带来影响
-5-   Palindrome.java
-6-   Chinese.java
-7-   TaskNo7.java

